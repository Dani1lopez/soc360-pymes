# Apply Progress: migrate-to-uv

## Summary

Post-apply remediation completed for review findings. The migration artifacts are in place; CI now guards lockfile freshness and uv import surface; README quality commands are uv-native; Docker assertions are limited to static coupling checks.

## TDD Cycle Evidence

All tasks in this change are covered by the focused SDD contract test file `tests/sdd/test_uv_migration.py`. Structural tasks that have a single deterministic output use that output as their only scenario; verification tasks that depend on running infrastructure are deferred to `sdd-verify` rather than marked as passed.

| Task | Test File | Layer | Safety Net | RED | GREEN | TRIANGULATE | REFACTOR |
|------|-----------|-------|------------|-----|-------|-------------|----------|
| 1.1 `pyproject.toml` metadata, deps, extras, dev | `tests/sdd/test_uv_migration.py` | Unit / static contract | ✅ baseline 12/12 | ✅ Written | ✅ Passed | ✅ 3 cases | ✅ Clean |
| 1.2 `.python-version` to `3.12` | `tests/sdd/test_uv_migration.py` | Unit / static contract | N/A (new file) | ✅ Written | ✅ Passed | ➖ Single output | ✅ Clean |
| 1.3 `uv.lock` generated and tracked | `tests/sdd/test_uv_migration.py` | Unit / static contract | N/A (new file) | ✅ Written | ✅ Passed | ➖ Single output | ✅ Clean |
| 2.1 Legacy headers on `requirements*.txt` | `tests/sdd/test_uv_migration.py` | Unit / static contract | ✅ baseline 12/12 | ✅ Written | ✅ Passed | ✅ 2 cases | ✅ Clean |
| 2.2 CI `test-uv` job | `tests/sdd/test_uv_migration.py` | Unit / static contract | ✅ baseline 12/12 | ✅ Written | ✅ Passed | ✅ 2 cases | ✅ Clean |
| 2.3 README / README.es uv quickstart | `tests/sdd/test_uv_migration.py` | Unit / static contract | ✅ baseline 12/12 | ✅ Written | ✅ Passed | ✅ 2 cases | ✅ Clean |
| 2.4 Docker/Compose static audit | `tests/sdd/test_uv_migration.py` | Unit / static contract | ✅ baseline 12/12 | ✅ Written | ✅ Passed | ✅ 2 cases | ✅ Clean |
| 3.1 uv sync + command smoke | `tests/sdd/test_uv_migration.py` | Unit / static contract + subprocess smoke | ✅ baseline 12/12 | ✅ Written | ✅ Passed locally for `uv sync`, `uv run pytest`, `uv run celery --help` | ➖ Infra-dependent commands deferred | ✅ Clean |
| 3.2 Extras import smoke | `tests/sdd/test_uv_migration.py` | Unit / static contract | ✅ baseline 12/12 | ✅ Written | ✅ Passed | ➖ Single scenario | ✅ Clean |
| 3.3 Legacy pip path | `tests/sdd/test_uv_migration.py` + fresh venv | Unit / static contract + manual | ✅ baseline 12/12 | ✅ Written | ✅ Passed in CI-equivalent fresh venv | ➖ Manual verification | ✅ Clean |
| 3.4 Docker/Compose service audit | `tests/sdd/test_uv_migration.py` | Unit / static contract | ✅ baseline 12/12 | ✅ Written | ✅ Passed | ➖ Single scenario | ✅ Clean |

## Implemented Tasks

- [x] `pyproject.toml`, `.python-version`, `uv.lock` created with preserved extras and deduped dev deps.
- [x] Legacy headers added to `requirements.txt` and `requirements-dev.txt`; pins and order preserved.
- [x] CI extended with parallel `test-uv` job.
- [x] README.md / README.es.md updated with uv quickstart and uv-native quality commands.
- [x] `tests/sdd/test_uv_migration.py` updated to assert lockfile freshness gate, uv import smoke step, and static Docker/Compose coupling checks.
- [x] Generated bytecode removed from `tests/sdd/__pycache__/`.

## Local Checks That Passed

| Check | Result |
|-------|--------|
| `uv sync --frozen --extra dev` | ✅ Passed locally |
| `uv lock --check` | ✅ Passed locally |
| `uv run pytest` | ✅ Passed locally |
| `uv run celery --help` | ✅ Passed locally |
| `uv run python -c "import uvicorn, sqlalchemy.ext.asyncio, redis.asyncio, celery, jose, passlib, asyncpg, cryptography, bcrypt, hiredis"` | ✅ Passed locally |
| SDD tests (`uv run pytest tests/sdd`) | ✅ Passed locally after remediation (14/14) |
| Python version contract (`.python-version == 3.12`, `requires-python = ">=3.12"`) | ✅ Passed locally |
| Static Docker/Compose audit (no Dockerfile, no `build:`, only `postgres`/`redis` services) | ✅ Passed |

## Checks Blocked by Missing Docker/Postgres/Redis or Pre-existing App Issues

| Check | Status | Reason |
|-------|--------|--------|
| `uv run ruff check .` | ⚠️ Out of CI / deferred | Pre-existing lint findings in the codebase; left as a `sdd-verify` check. |
| `uv run mypy .` | ⚠️ Out of CI / deferred | Pre-existing type-check findings; left as a `sdd-verify` check. |
| `uv run alembic upgrade head` | ⚠️ Out of CI / deferred | Requires running Postgres/Redis; left as a `sdd-verify` check. |
| Runtime `docker compose --profile dev up -d` | ⚠️ Deferred | Local Docker/Postgres/Redis availability not guaranteed; static compose audit passed. |

## Checks Deferred to `sdd-verify`

- Full `ruff` / `mypy` cleanliness under uv.
- Alembic migration up/down cycle under uv.
- Celery worker start / task execution under uv with a live broker.
- End-to-end Docker Compose runtime health check.
- Python version mismatch runtime proof on an interpreter older than 3.12.

## Changed Files

| File | Action |
|------|--------|
| `.github/workflows/ci.yml` | Added `uv lock --check`, extras/import smoke step, and `uv run celery --help` smoke step to `test-uv`. |
| `tests/sdd/test_uv_migration.py` | Added assertions for lockfile freshness, import smoke, Celery CLI smoke, and static compose services. |
| `README.md` | Added uv prerequisite; quality commands now use `uv run`. |
| `README.es.md` | Added uv prerequisite; quality commands now use `uv run`. |
| `openspec/changes/migrate-to-uv/proposal.md` | Updated risk and success criteria to `uv run celery --help`. |
| `openspec/changes/migrate-to-uv/specs/dependency-management/spec.md` | Updated command-surface scenario to `uv run celery --help`; refined Python version contract scenario. |
| `openspec/changes/migrate-to-uv/design.md` | Updated smoke command surface to `uv run celery --help`. |
| `openspec/changes/migrate-to-uv/tasks.md` | Marked tasks complete and annotated blocked/deferred checks. |
| `openspec/changes/migrate-to-uv/apply-progress.md` | Created (this file). |
| `tests/sdd/__pycache__/*.pyc` | Removed generated bytecode. |

## Status

Ready for formal `sdd-verify`. CI gates cover the uv-specific surface that can be enforced in a clean GitHub Actions environment; heavier checks that need running infrastructure or touch pre-existing app issues remain verify-phase checks.
